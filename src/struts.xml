 <!DOCTYPE struts PUBLIC "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"  "http://struts.apache.org/dtds/struts-2.3.dtd">


<struts>
	<constant name="struts.devMode" value="true" />
	<package name="login" extends="struts-default" namespace="/LoginUser">
		<interceptors>
			<interceptor class="com.sun.interceptor.LoginInterceptor" name="loginInterceptor">
			</interceptor>
			<interceptor-stack name="loginStack">
				<interceptor-ref name="loginInterceptor" />
				<interceptor-ref name="defaultStack" />
			</interceptor-stack>
		</interceptors>

		<global-results>
			<result name="login">/login.jsp</result>
		</global-results>

		<!--   login action  -->
		<action name="loginUser" class="com.sun.actions.LoginAction" method="loginRegisterUser">
			<result name="success" type="redirect">homeAction.action</result>
			<result name="login">/login.jsp</result>
		</action>

		<!-- home link action -->
		<action name="homeAction" class="com.sun.actions.LoginAction" method="home">
			<interceptor-ref name="loginStack" />
			<result name="success">/user.jsp</result>
		</action>
		<!--   logout action  -->
		<action name="logOut" class="com.sun.actions.LoginAction" method="logOut">
			<result name="success">/login.jsp</result>
		</action>
	</package>
</struts>